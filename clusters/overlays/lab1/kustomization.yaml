apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- ../../base

resources:
- htpasswd-secret.yaml
- gitops-users-group.yaml
- namespace.yaml

# patches:
# - path: oauth-patch.yaml
#   target:
#     kind: OAuth
#     name: cluster

